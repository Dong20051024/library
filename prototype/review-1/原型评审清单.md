# 原型评审清单 —— 图书馆座位预约系统

> **评审轮次**：Review-1 | **评审日期**：2026-07-07 | **原型版本**：static-v1
>
> 逐项检查以下条目，在对应单元格打 ✅ / ❌ / △（△表示部分满足但需改进）。

---

## 1. 核心用户流程是否完整

> 评审目标：从学生打开首页到完成预约的全链路是否闭环。

| # | 检查项 | 涉及页面 | 结果 | 备注 |
|---|--------|----------|------|------|
| 1.1 | 首页 → 座位列表 跳转是否可用 | index.html → seats.html | | |
| 1.2 | 座位列表 → 座位详情 跳转是否可用 | seats.html → seat-detail.html | | |
| 1.3 | 座位详情 → 预约提交 跳转是否可用 | seat-detail.html → reservation-create.html | | |
| 1.4 | 预约提交 → 我的预约 是否自动跳转 | 确认按钮触发跳转 | | |
| 1.5 | 我的预约 → 取消预约 弹窗流程是否可用 | 取消按钮 → 确认弹窗 → 成功提示 | | |
| 1.6 | 管理员登录 → 预约管理 跳转是否可用 | login.html → reservations.html | | |
| 1.7 | 预约管理 → 签到 / 释放 按钮是否可操作 | 签到按钮 → Toast 提示 | | |
| 1.8 | 预约管理 → 座位管理 → 统计 侧边栏跳转 | 左侧菜单切换各页面 | | |

**问题记录**：

---

## 2. 页面跳转逻辑是否清楚

> 评审目标：用户在每个页面是否清楚"我从哪来、可以到哪去、当前在哪"。

| # | 检查项 | 涉及页面 | 结果 | 备注 |
|---|--------|----------|------|------|
| 2.1 | 所有用户端页面顶部导航是否一致，当前页是否有标识 | 全部用户端页面 | | |
| 2.2 | 所有管理端页面侧边栏选中态是否正确 | 全部管理端页面 | | |
| 2.3 | 是否有页面没有返回入口（死胡同页面） | 全部页面逐一检查 | | |
| 2.4 | 用户端的返回按钮是否指向正确的上一个页面 | seats → index, seat-detail → seats, create → seat-detail | | |
| 2.5 | 管理员登录成功后跳转目标是否正确 | login → reservations | | |
| 2.6 | 退出登录后是否能回到登录页 | 侧边栏"退出登录"链接 | | |
| 2.7 | 体验账号切换后的跳转是否合理 | 切换→跳转首页 | | |

**问题记录**：

---

## 3. 空状态和错误状态是否考虑

> 评审目标：异常路径是否有对应的 UI 承载，而不是留白或崩溃。

| # | 检查项 | 涉及页面 | 结果 | 备注 |
|---|--------|----------|------|------|
| 3.1 | 我的预约页有无预约记录时的空状态 | my-reservations.html（空状态切换） | | |
| 3.2 | 座位管理页无座位数据时的空状态 | seats-management.html（空状态切换） | | |
| 3.3 | 统计页数据不足时的状态 | statistics.html（底部数据不足演示） | | |
| 3.4 | 管理员登录密码错误时的错误提示 | login.html（错误状态切换） | | |
| 3.5 | 预约提交时段冲突的错误提示 | reservation-create.html（底部冲突演示） | | |
| 3.6 | 座位不存在或已被删除的异常提示 | seat-detail.html（底部异常演示） | | |
| 3.7 | 删除座位时有关联预约记录的阻止提示 | seats-management.html（禁用删除按钮） | | |
| 3.8 | 筛选后无匹配结果是否有提示 | 预留，当前原型未实现筛选后空结果 | | |

**问题记录**：

---

## 4. 手机端和桌面端是否区分

> 评审目标：用户端的手机布局和管理端的桌面布局是否正确。

| # | 检查项 | 涉及页面 | 结果 | 备注 |
|---|--------|----------|------|------|
| 4.1 | 用户端页面在手机竖屏（<576px）是否单列、按钮全宽 | 全部用户端页面 | | |
| 4.2 | 用户端页面在电脑宽屏（≥992px）是否居中、间距合理 | 全部用户端页面 | | |
| 4.3 | 座位卡片在手机上是否 2 列显示 | seats.html | | |
| 4.4 | 管理端页面在桌面端（≥1200px）左侧菜单是否固定 | 全部管理端页面 | | |
| 4.5 | 管理端表格在桌面端是否需要横向滚动 | reservations.html, seats-management.html | | |
| 4.6 | 管理端页面在小屏（<768px）是否可读 | 全部管理端页面 | | |
| 4.7 | 表单输入框在手机上是否高度 ≥ 48px | reservation-create.html, login.html | | |

**问题记录**：

---

## 5. 哪些问题必须在接后端前修改

> 根据以上检查，汇总必须在进入 MVC 开发前解决的问题。

| # | 问题描述 | 所属页面 | 严重程度 | 修复建议 |
|---|----------|----------|----------|----------|
| 1 | | | [高/中/低] | |
| 2 | | | | |
| 3 | | | | |
| 4 | | | | |
| 5 | | | | |

### 严重程度说明

| 程度 | 含义 | 处理要求 |
|------|------|----------|
| **高** | 功能链路断裂或关键信息不可见 | 必须在本轮修复，否则影响后续开发 |
| **中** | 体验不够完善，但不影响核心功能理解 | 建议修复，可延至下一轮迭代 |
| **低** | 视觉细节或文案润色 | 可以不改，在 MVC 开发中自然修正 |

---

## 评审结论

| 项目 | 内容 |
|------|------|
| 原型版本 | static-v1 |
| 本轮评审人 | （待填写） |
| 评审日期 | （待填写） |
| 总体评价 | [通过 / 有条件通过 / 需重审] |
| 高优先级问题数 | |
| 中优先级问题数 | |
| 低优先级问题数 | |
| 下一步动作 | [进入 MVC 开发 / 修复问题后重审] |

---

*评审清单版本：v1.0 | 对应原型：prototype/static-v1/*  
*使用方式：逐项检查后在"结果"列填入 ✅/❌/△，在备注栏注明具体问题，最后汇总第 5 节。*
